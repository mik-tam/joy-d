# Create a local .env file from this template. Never commit the real key.
# Production hosts commonly set PORT automatically. `npm start` binds to the
# host's network interface; local development remains loopback-only.
PORT=8787

# Recommended for JOY:D (OpenRouter credits — no OpenAI API balance needed):
# - Stories: OpenRouter free tier by default (openrouter/free, NOT openai/* —
#   those often return ToS 403). Free tier can be slow; set OPENAI_API_KEY too
#   and a slow free-tier response automatically falls back to direct OpenAI
#   for just that one request, so OpenAI credits are only spent when needed.
# - Images: OpenRouter image model
OPENROUTER_API_KEY=
OPENROUTER_MODEL=openrouter/free
OPENROUTER_IMAGE_MODEL=openai/gpt-image-1-mini
OPENROUTER_SITE_URL=https://joy-d-one.vercel.app

JOYD_WORLD_IMAGES=on
JOYD_TEXT_PROVIDER=openrouter
JOYD_IMAGE_PROVIDER=openrouter

# Optional direct OpenAI. Used as the automatic slow-response fallback above,
# or set JOYD_TEXT_PROVIDER=openai to use it for every story instead:
# OPENAI_API_KEY=
# OPENAI_MODEL=gpt-5.6
# OPENAI_IMAGE_MODEL=gpt-image-1
# JOYD_TEXT_PROVIDER=openai

# Real anonymous smile matching (optional locally, required for production
# matching). Run supabase/migrations/20260721_create_joyd_match_profiles.sql
# in the Supabase SQL editor, then set these server-only values. Never expose
# the service-role key in a VITE_* variable.
# Use the project URL only — do NOT include /rest/v1/
# Example: https://YOUR_PROJECT_REF.supabase.co
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
